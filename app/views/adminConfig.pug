<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
//- <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

</head>

<!------ Include the above in your HEAD tag ---------->
head  
    style.
        .title{
            margin-top: 15px ;
            font-weight: bold ;
        }
        .report{
            margin: 0 ;
            position: absolute ;
            top: 20% ;
            left: 50% ;
            margin-right: -50% ;
            transform: translate(-50%, -50%) ;
            border-style: dashed ;
            padding-top: 50px;
            padding-right: 50px;
            padding-bottom: 50px;
            padding-left: 50px;
            margin-top: 13% ;
            font-size:20px;
            border-radius: 12px;
        }
        .noData{
        padding-top:40px;
        font-size:25px;
        }

        .licensePlateColumn
        {
        width: 20;
        }
        .sidenav {
        height: 100%;
        width: 17%;
        position: fixed;
        z-index: 1;
        top: 0;
        left: 0;
        background-color: #111;
        overflow-x: hidden;
        padding-top: 20px;
        
        }

        .sidenav a {
        padding: 6px 8px 6px 16px;
        text-decoration: none;
        font-size: 25px;
        color: #818181;
        display: block;
        }

        .sidenav a:hover {
        color: #f1f1f1;
        }
        .tableHeader{
        font-size: 50px;
        padding-left:50%;
        padding-top:1%;
        }
        .table-responsive{
        margin-top: -1.5% ;
        
        }
        .reportBox{
        overflow-x:auto;
        margin-left:21%;
        }
        .adminSubmit{
        margin-left:35%;
        }
        .statusActive{
        color:green;
        }
<!---------------------------------------------------->

<div class="sidenav">
  <div class="sidebar-header">
            <h3>การจัดการ</h3>
  </div>
  <ul>
    <li class="active"><a href="/admin/report?link=0">ข้อมูลออเดอร์ทั้งหมด</a></li>
    <li><a href="/admin/report?link=1">ออเดอร์ที่รอการอนุมัติ</a></li>
    <li><a href="/admin/report?link=2">ออเดอร์ที่ได้รับการอนุมัติ</a></li>
    <li><a href="/admin/report?link=3">ออเดอร์ที่กำลังไปรับของ</a></li>
    <li><a href="/admin/report?link=4">ออเดอร์ที่กำลังขนส่ง</a></li>
    <li><a href="/admin/report?link=5">ออเดอร์ที่เสร็จสิ้น</a></li>
  </ul>
</div>

if transportType1
    <form action="/admin/edit?order=#{data.orderNo}" method=post>
        <div class='report'>
            <p><span class='title'> หมายเลขออเดอร์ : </span><span>#{data.orderNo}</span></p>
            <p><span class='title'> userID : </span><span>#{data.userID}</span></p>
            <p><span class='title'> ชื่อ-นามสกุล : </span><span>#{data.fullname}</span></p>
            <p><span class='title'> เบอร์โทร : </span><span>#{data.tel}</span></p>
            <p><span class='title'> ที่อยู่1 : </span><span>#{data.address1}</span></p>
            <p><span class='title'> ที่อยู่2 : </span><span>#{data.address2}</span></p>
            <p><span class='title'> ชนิดของพาเลท : </span><span>#{data.palletsize}</span><span class='title'>&nbsp; จำนวน :</span><span> #{data.qty} </span></p>
            <p><span class='title'> รุ่นของรถบรรทุก : </span><span>#{data.truckType}</span></p>
            <p><span class='title'> ทะเบียนรถ : </span><span>#{data.licensePlate}</span></p>
            <p><span class='title'> วัน/เดือน/ปี : </span><span>#{data.time}</span></p>
            
            if status1
                <p><span class='title'> สถานะ : </span>
                <select name="status">
                    <option value=#{data.status}>#{data.status}</option>
                    <option value="ได้รับการอนุมัติ">ได้รับการอนุมัติ</option>
                    <option value="กำลังไปรับของ">กำลังไปรับของ</option>
                    <option value="กำลังขนส่ง">กำลังขนส่ง</option>
                    <option value="เสร็จสิ้น">เสร็จสิ้น</option>
                </select><p class="adminSubmit">
            if status2
                <p><span class='title'> สถานะ : </span>
                <select name="status">
                    <option value=#{data.status}>#{data.status}</option>
                    <option value="รอการอนุมัติ">รอการอนุมัติ</option>
                    <option value="กำลังไปรับของ">กำลังไปรับของ</option>
                    <option value="กำลังขนส่ง">กำลังขนส่ง</option>
                    <option value="เสร็จสิ้น">เสร็จสิ้น</option>
                </select><p class="adminSubmit">
            if status3
                <p><span class='title'> สถานะ : </span>
                <select name="status">
                    <option value=#{data.status}>#{data.status}</option>
                    <option value="รอการอนุมัติ">รอการอนุมัติ</option>
                    <option value="ได้รับการอนุมัติ">ได้รับการอนุมัติ</option>
                    <option value="กำลังขนส่ง">กำลังขนส่ง</option>
                    <option value="เสร็จสิ้น">เสร็จสิ้น</option>
                </select><p class="adminSubmit">
            if status4
                <p><span class='title'> สถานะ : </span>
                <select name="status">
                    <option value=#{data.status}>#{data.status}</option>
                    <option value="รอการอนุมัติ">รอการอนุมัติ</option>
                    <option value="ได้รับการอนุมัติ">ได้รับการอนุมัติ</option>
                    <option value="กำลังไปรับของ">กำลังไปรับของ</option>
                    <option value="เสร็จสิ้น">เสร็จสิ้น</option>
                </select><p class="adminSubmit">
            if status5
                <p><span class='title'> สถานะ : </span>
                <select name="status">
                    <option value=#{data.status}>#{data.status}</option>
                    <option value="รอการอนุมัติ">รอการอนุมัติ</option>
                    <option value="ได้รับการอนุมัติ">ได้รับการอนุมัติ</option>
                    <option value="กำลังไปรับของ">กำลังไปรับของ</option>
                    <option value="กำลังขนส่ง">กำลังขนส่ง</option>
                </select><p class="adminSubmit">
            
            <input type=submit value="ยืนยันการขนส่ง"></p>
        </div>
    </form>
if transportType2
    <form action="/admin/edit?order=#{data.orderNo}" method=post>
        <div class='report'>
            <p><span class='title'> หมายเลขออเดอร์ : </span><span>#{data.orderNo}</span></p>
            <p><span class='title'> userID : </span><span>#{data.userID}</span></p>
            <p><span class='title'> ชื่อ-นามสกุล : </span><span>#{data.fullname}</span></p>
            <p><span class='title'> เบอร์โทร : </span><span>#{data.tel}</span></p>
            <p><span class='title'> ที่อยู่1 : </span><span>#{data.address1}</span></p>
            <p><span class='title'> ที่อยู่2 : </span><span>#{data.address2}</span></p>
            <p><span class='title'> ชนิดของพาเลท : </span><span>#{data.palletsize}</span><span class='title'>&nbsp; จำนวน :</span><span> #{data.qty} </span></p>
            <p><span class='title'> รุ่นของรถบรรทุก : </span><span>#{jsonarraytype}</span></p>
            <p><span class='title'> ทะเบียนรถ : </span><span>#{jsonarraylicense}</span></p>
            <p><span class='title'> วัน/เดือน/ปี : </span><span>#{data.time}</span></p>
            if status1
                <p><span class='title'> สถานะ : </span><select name="status">
                    <option value=#{data.status}>#{data.status}</option>
                    <option value="ได้รับการอนุมัติ">ได้รับการอนุมัติ</option>
                    <option value="กำลังไปรับของ">กำลังไปรับของ</option>
                    <option value="กำลังขนส่ง">กำลังขนส่ง</option>
                    <option value="เสร็จสิ้น">เสร็จสิ้น</option>
                </select><p class="adminSubmit">
            if status2
                <p><span class='title'> สถานะ : </span><select name="status">
                    <option value=#{data.status}>#{data.status}</option>
                    <option value="รอการอนุมัติ">รอการอนุมัติ</option>
                    <option value="กำลังไปรับของ">กำลังไปรับของ</option>
                    <option value="กำลังขนส่ง">กำลังขนส่ง</option>
                    <option value="เสร็จสิ้น">เสร็จสิ้น</option>
                </select><p class="adminSubmit">
            if status3
                <p><span class='title'> สถานะ : </span><select name="status">
                    <option value=#{data.status}>#{data.status}</option>
                    <option value="รอการอนุมัติ">รอการอนุมัติ</option>
                    <option value="ได้รับการอนุมัติ">ได้รับการอนุมัติ</option>
                    <option value="กำลังขนส่ง">กำลังขนส่ง</option>
                    <option value="เสร็จสิ้น">เสร็จสิ้น</option>
                </select><p class="adminSubmit">
            if status4
                <p><span class='title'> สถานะ : </span><select name="status">
                    <option value=#{data.status}>#{data.status}</option>
                    <option value="รอการอนุมัติ">รอการอนุมัติ</option>
                    <option value="ได้รับการอนุมัติ">ได้รับการอนุมัติ</option>
                    <option value="กำลังไปรับของ">กำลังไปรับของ</option>
                    <option value="เสร็จสิ้น">เสร็จสิ้น</option>
                </select><p class="adminSubmit">
            if status5
                <p><span class='title'> สถานะ : </span><select name="status">
                    <option value=#{data.status}>#{data.status}</option>
                    <option value="รอการอนุมัติ">รอการอนุมัติ</option>
                    <option value="ได้รับการอนุมัติ">ได้รับการอนุมัติ</option>
                    <option value="กำลังไปรับของ">กำลังไปรับของ</option>
                    <option value="กำลังขนส่ง">กำลังขนส่ง</option>
                </select><p class="adminSubmit">
            
            <input type=submit value="ยืนยันการขนส่ง"></p>
        </div>
    </form>